# 프롬프트 엔지니어링과 RAG 발표 흐름도

## 발표 주제

**AI에게 질문만 하지 말고, 맥락을 주자**  
부제: 프롬프트 엔지니어링과 RAG로 더 좋은 답변 얻기

---

## 전체 핵심 메시지

LLM은 입력된 **맥락**을 바탕으로 답변을 생성한다.  
따라서 좋은 답변을 얻으려면 좋은 질문뿐 아니라, 적절한 맥락을 함께 제공해야 한다.

**발표의 중심 흐름**

```text
LLM은 맥락을 보고 답한다
→ 그래서 프롬프트 엔지니어링이 중요하다
→ 하지만 사람이 매번 맥락을 넣기는 어렵다
→ RAG는 필요한 맥락을 자동으로 찾아준다
```

---

## 1. 도입: LLM은 일상 도구가 되었다

요즘 많은 사람들이 ChatGPT, Gemini 같은 LLM을 사용한다.

- ChatGPT: 주간 활성 사용자 약 9억 명
- Gemini: 월간 활성 사용자 약 7억 5천만 명

이제 LLM은 일부 개발자나 연구자만 쓰는 도구가 아니라, 글쓰기, 검색, 요약, 코딩, 학습 등 일상에서 쓰이는 도구가 되었다.

하지만 사용하다 보면 항상 좋은 답변이 나오지는 않는다.

- 너무 일반적인 답변
- 내가 원한 방향과 다른 답변
- 근거가 부족한 답변
- 바로 쓰기 어려운 답변

**다음 질문으로 연결**

> 왜 같은 AI를 쓰는데도 답변 품질이 달라질까?

---

## 2. 문제 제기: 답변 품질은 입력에 영향을 받는다

LLM은 사용자의 머릿속 의도를 직접 읽는 도구가 아니다.  
사용자가 입력한 문장과 맥락을 바탕으로 답변을 생성한다.

따라서 답변 품질은 모델 성능뿐 아니라, 사용자가 어떤 입력을 주는지에도 영향을 받는다.

**핵심 문장**

> 좋은 답변을 얻으려면, 좋은 입력을 설계해야 한다.

---

## 3. LLM 원리 최소 설명

LLM은 입력된 문맥을 바탕으로 다음에 올 가능성이 높은 단어 또는 토큰을 예측하며 답변을 생성한다.

여기서 중요한 점은 다음과 같다.

- 앞에 어떤 문장이 주어졌는지
- 어떤 조건이 주어졌는지
- 어떤 참고 자료가 함께 제공되었는지

이 요소들이 답변의 방향과 품질에 영향을 준다.

**비유**

> LLM은 시험을 치는 학생과 비슷하다.  
> 문제만 주면 일반적인 답을 쓰지만, 채점 기준과 참고자료, 답안 형식을 함께 주면 더 원하는 답에 가까워진다.

**다음 내용으로 연결**

> 그렇다면 우리는 LLM에게 어떤 식으로 입력을 줘야 할까?

---

## 4. 프롬프트 엔지니어링

프롬프트 엔지니어링은 LLM이 사용자의 의도에 더 가까운 답변을 생성하도록 입력을 설계하는 과정이다.

좋은 프롬프트에는 보통 다음 요소가 들어간다.

| 요소 | 설명 |
|---|---|
| 역할 | 어떤 관점에서 답할지 |
| 목표 | 무엇을 얻고 싶은지 |
| 조건 | 지켜야 할 제한사항 |
| 맥락 | 참고해야 할 배경 정보 |
| 출력 형식 | 표, 목록, 발표문 등 |
| 예시 | 원하는 답변 스타일 |

예시 비교:

```text
나쁜 예시:
RAG 설명해줘.

좋은 예시:
비전공자도 이해할 수 있도록 RAG를 설명해줘.
프롬프트 엔지니어링과의 관계를 포함하고,
발표 슬라이드에 넣을 수 있게 3문장으로 정리해줘.
```

**핵심 문장**

> 프롬프트 엔지니어링은 AI에게 예쁘게 질문하는 기술이 아니라, 원하는 답변이 나오도록 입력 조건을 설계하는 기술이다.

---

## 5. 핵심 전환: 맥락이 중요하다

좋은 프롬프트에서 특히 중요한 것은 **맥락**이다.

질문만 던지면 LLM은 일반적인 답변을 생성할 가능성이 높다.  
하지만 목적, 대상, 자료, 조건을 함께 제공하면 답변은 훨씬 구체적이고 유용해진다.

예를 들어 다음 질문만 하면 답변이 일반적일 수 있다.

```text
발표 주제 추천해줘.
```

하지만 아래 맥락을 함께 주면 답변이 달라진다.

```text
대상: 비전공자 포함 대학생
주제 범위: Microsoft Learn 기반 AI 기술
조건: 데모 포함
발표 시간: 10분
관심 주제: 프롬프트 엔지니어링과 RAG
```

**핵심 문장**

> 좋은 답변은 좋은 질문에서 시작하지만, 더 좋은 답변은 좋은 맥락에서 완성된다.

---

## 6. RAG 설명

RAG는 여러 자료를 미리 넣어두고, 사용자의 질문과 관련 있는 자료를 검색한 뒤, 그 내용을 프롬프트의 맥락으로 추가해서 답변하게 하는 방식이다.

흐름은 다음과 같다.

```text
자료 업로드
→ 문서 조각화
→ 검색 가능한 형태로 저장
→ 사용자 질문
→ 관련 자료 검색
→ 검색된 내용을 프롬프트에 추가
→ LLM 답변 생성
```

쉽게 말하면:

> RAG는 AI에게 자료실과 사서를 붙여주는 방식이다.  
> 질문이 들어오면 관련 자료를 먼저 찾고, 그 자료를 바탕으로 답변한다.

**핵심 문장**

> RAG는 좋은 프롬프트에 필요한 맥락을 자동으로 찾아 붙여주는 방식이다.

---

## 7. Demo 전 관전 포인트

데모에서는 모델 자체가 바뀌는 것이 아니라, **맥락이 추가되었을 때 답변이 어떻게 달라지는지**를 보여준다.

비교 구조:

```text
그냥 프롬프트
→ 모델의 일반 지식에 의존
→ 일반적인 답변

RAG 기반 프롬프트
→ 관련 자료 검색
→ 자료를 맥락으로 추가
→ 더 구체적이고 근거 있는 답변
```

**관전 포인트**

> 같은 질문이라도, LLM에게 어떤 맥락을 주느냐에 따라 답변 품질이 달라진다.

---

## 8. Demo 흐름

### 1단계: 그냥 프롬프트만 입력

```text
프롬프트 엔지니어링을 잘하는 방법을 알려줘.
```

예상 결과:

- 명확하게 질문하세요
- 역할을 지정하세요
- 예시를 주세요
- 출력 형식을 정하세요

평가:

> 틀린 답은 아니지만 너무 일반적이고, 발표 목적에 맞게 정리되어 있지 않다.

### 2단계: RAG로 맥락을 추가한 프롬프트

```text
Microsoft Learn의 프롬프트 엔지니어링 자료와 RAG 자료를 참고해서,
비전공자 포함 대학생 대상 발표용으로
프롬프트 엔지니어링과 RAG의 관계를 설명해줘.

조건:
- 일상적 경험에서 시작할 것
- LLM 원리와 연결할 것
- 맥락의 중요성을 강조할 것
- 발표 슬라이드용 문장으로 정리할 것
```

예상 결과:

- 발표 목적에 맞는 답변
- 자료 기반 설명
- 프롬프트 엔지니어링과 RAG의 자연스러운 연결
- 바로 슬라이드에 옮기기 쉬운 정리

**Demo 결론**

> 좋은 답변이 나온 이유는 질문이 길어졌기 때문이 아니라, 질문과 관련된 맥락이 함께 제공되었기 때문이다.

---

## 9. 활용 가능성

단순히 ChatGPT에 파일을 넣고 질문하는 방식도 개인 사용에는 충분히 편리하다.

하지만 자료가 많아지고, 반복적으로 사용하고, 여러 사람이 함께 써야 한다면 RAG가 더 적합하다.

| 방식 | 장점 | 한계 |
|---|---|---|
| 파일 업로드 후 질문 | 간단하고 빠름 | 자료가 많아지면 관리가 어려움 |
| RAG | 관련 자료를 자동 검색하고 확장성이 좋음 | 초기 구성이 필요함 |

활용 예시:

- 수업 자료 Q&A
- 회사 매뉴얼 챗봇
- 논문 읽기 보조
- 동아리 문서 검색
- 고객 문의 자동화

**핵심 문장**

> 개인 실험은 파일 업로드로 충분할 수 있지만, 자료가 많고 반복적으로 사용해야 하는 환경에서는 RAG가 더 적합하다.

---

## 10. 정리

발표의 결론은 다음과 같다.

```text
1. LLM은 이미 일상적인 도구가 되었다.
2. 하지만 원하는 답변이 항상 나오는 것은 아니다.
3. LLM은 입력된 맥락을 바탕으로 답변을 생성한다.
4. 그래서 프롬프트 엔지니어링이 필요하다.
5. 좋은 프롬프트의 핵심은 명확한 지시와 적절한 맥락이다.
6. RAG는 필요한 맥락을 자료에서 검색해 프롬프트에 추가하는 방식이다.
7. 결국 더 좋은 AI 활용은 더 좋은 맥락 설계에서 나온다.
```

마지막 문장 후보:

> AI에게 좋은 답을 얻고 싶다면, 좋은 질문뿐 아니라 좋은 맥락을 함께 줘야 한다.

또는:

> 이제 중요한 것은 AI를 쓰는가가 아니라, AI에게 어떤 맥락을 주고 어떻게 쓰는가이다.

---

## 최종 흐름 요약

```text
[도입]
LLM은 수억 명이 쓰는 일상 도구가 되었다.
하지만 원하는 답변이 항상 나오지는 않는다.

↓
[문제 제기]
왜 같은 AI인데 답변 품질이 달라질까?

↓
[LLM 원리]
LLM은 입력된 맥락을 바탕으로 답변을 생성한다.

↓
[프롬프트 엔지니어링]
원하는 답변을 얻기 위해 역할, 목표, 조건, 형식, 예시, 맥락을 설계한다.

↓
[핵심 전환]
좋은 프롬프트에는 맥락이 중요하다.
하지만 사람이 매번 적절한 자료를 찾아 넣기는 어렵다.

↓
[RAG]
RAG는 사용자의 자료 중 질문과 관련된 내용을 검색해
프롬프트의 맥락으로 추가하는 방식이다.

↓
[Demo]
그냥 프롬프트만 넣은 답변과
RAG로 맥락을 추가한 답변을 비교한다.

↓
[활용 가능성]
자료가 많고 반복적으로 사용해야 하는 환경에서는
RAG가 유지보수, 확장성, 근거 기반 답변 측면에서 유리하다.

↓
[정리]
좋은 답변은 좋은 질문에서 시작하지만,
더 좋은 답변은 좋은 맥락에서 완성된다.
```
